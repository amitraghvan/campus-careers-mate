/**
 * useOpportunities — convenience re-export of the context hook.
 * Components import from here instead of directly from context.
 */

export { useOpportunityContext as useOpportunities } from "@/features/opportunities/contexts/OpportunityContext";
